import * as React from 'react';

interface ICounterPickerProps {
}

const CounterPicker: React.FunctionComponent<ICounterPickerProps> = (props) => {
  return <h1>CounterPicker</h1>;
};

export default CounterPicker;
